<nav class="navbar bg-base-100/90 backdrop-blur-md border-b border-base-200 sticky top-0 z-40 px-4 sm:px-8">
    <!-- Brand / Mobile Toggle -->
    <div class="navbar-start gap-2">
        <div class="dropdown lg:hidden">
            <label tabindex="0" class="btn btn-ghost btn-circle btn-sm">
                <svg xmlns="http://www.w3.org/2000/svg" class="h-5 w-5" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 6h16M4 12h16M4 18h7" />
                </svg>
            </label>
            <ul tabindex="0" class="menu menu-sm dropdown-content mt-3 z-[1] p-2 shadow-lg bg-base-100 rounded-box w-52 border border-base-200">
                <li><a href="/" class="<%= typeof path !== 'undefined' && path === '/' ? 'active' : '' %>">Home Hub</a></li>
                <li><a href="/dashboard" class="<%= typeof path !== 'undefined' && path === '/dashboard' ? 'active' : '' %>">Dashboard</a></li>
                <li><a href="/fiddle" class="<%= typeof path !== 'undefined' && path === '/fiddle' ? 'active' : '' %>">Code Editor</a></li>
                <% if (typeof user !== 'undefined' && user && user.role === 'admin') { %>
                    <li><a href="/admin" class="<%= typeof path !== 'undefined' && path === '/admin' ? 'active' : '' %>">Admin Panel</a></li>
                <% } %>
            </ul>
        </div>
        <a href="/" class="flex items-center gap-2.5 font-bold text-lg tracking-tight hover:opacity-80 transition-opacity">
            <div class="w-8 h-8 rounded-lg bg-primary text-primary-content flex items-center justify-center font-extrabold shadow-sm text-sm">
                A
            </div>
            <span class="text-base-content font-bold">Apps<span class="text-primary">Loader</span></span>
        </a>
    </div>

    <!-- Desktop Navigation Links -->
    <div class="navbar-center hidden lg:flex">
        <ul class="menu menu-horizontal px-1 gap-1 font-medium text-sm">
            <li>
                <a href="/" class="rounded-lg <%= typeof path !== 'undefined' && path === '/' ? 'bg-primary/10 text-primary font-semibold' : 'text-base-content/80' %>">
                    <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3 12l2-2m0 0l7-7 7 7M5 10v10a1 1 0 001 1h3m10-11l2 2m-2-2v10a1 1 0 01-1 1h-3m-6 0a1 1 0 001-1v-4a1 1 0 011-1h2a1 1 0 011 1v4a1 1 0 001 1m-6 0h6"/></svg>
                    Home
                </a>
            </li>
            <li>
                <a href="/dashboard" class="rounded-lg <%= typeof path !== 'undefined' && path === '/dashboard' ? 'bg-primary/10 text-primary font-semibold' : 'text-base-content/80' %>">
                    <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 6a2 2 0 012-2h2a2 2 0 012 2v2a2 2 0 01-2 2H6a2 2 0 01-2-2V6zM14 6a2 2 0 012-2h2a2 2 0 012 2v2a2 2 0 01-2 2h-2a2 2 0 01-2-2V6zM4 16a2 2 0 012-2h2a2 2 0 012 2v2a2 2 0 01-2 2H6a2 2 0 01-2-2v-2zM14 16a2 2 0 012-2h2a2 2 0 012 2v2a2 2 0 01-2 2h-2a2 2 0 01-2-2v-2z"/></svg>
                    Dashboard
                </a>
            </li>
            <li>
                <a href="/fiddle" class="rounded-lg <%= typeof path !== 'undefined' && path === '/fiddle' ? 'bg-primary/10 text-primary font-semibold' : 'text-base-content/80' %>">
                    <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M10 20l4-16m4 4l4 4-4 4M6 16l-4-4 4-4"/></svg>
                    Code Editor
                </a>
            </li>
        </ul>
    </div>

    <!-- User Menu & Actions -->
    <div class="navbar-end gap-2">
        <% if (typeof user !== 'undefined' && user) { %>
            <% if (user.role === 'admin') { %>
                <a href="/admin" class="btn btn-xs btn-neutral rounded-full px-3">Admin</a>
            <% } %>

            <div class="flex items-center gap-2 pl-2 border-l border-base-200">
                <div class="hidden sm:flex flex-col text-right">
                    <span class="text-xs font-semibold text-base-content leading-tight"><%= user.username %></span>
                    <% if (user.is_approved) { %>
                        <span class="text-[10px] text-success font-medium">Verified</span>
                    <% } else { %>
                        <span class="text-[10px] text-warning font-medium">Pending</span>
                    <% } %>
                </div>
                <div class="avatar placeholder">
                    <div class="bg-primary/10 text-primary rounded-full w-8 h-8 text-xs font-bold ring-1 ring-primary/20">
                        <%= (user.username || 'U').charAt(0).toUpperCase() %>
                    </div>
                </div>
                <a href="/logout" class="btn btn-ghost btn-circle btn-sm text-base-content/60 hover:text-error" title="Logout">
                    <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17 16l4-4m0 0l-4-4m4 4H7m6 4v1a3 3 0 01-3 3H6a3 3 0 01-3-3V7a3 3 0 013-3h4a3 3 0 013 3v1"/></svg>
                </a>
            </div>
        <% } else { %>
            <a href="/login" class="btn btn-ghost btn-sm">Login</a>
            <a href="/register" class="btn btn-primary btn-sm">Register</a>
        <% } %>
    </div>
</nav>